> **核心定位**: 本轮不铺新模块，专注打磨用户与内容交互的三个高频触点——阅读、编辑、采集。让博客阅读与写作不再是两件分离的事，让在线文档教程转化为本地知识资产只需一键，让章节导航变得直觉而精准。  
> **原则**: 零新依赖（HTML 解析复用现有 cheerio），零 Schema 变更，所有改动围绕现有页面结构做就地增强，每项功能都直接解决一个明确的用户痛点。

* * *

## **一、提案总览**

**任务名称类型工时解决的核心痛点**T1601**博客阅读即编辑** — 内嵌编辑器 + 位置同步交互变革4h长文修改需跳转、找回位置，打断思维T1602**在线手册一键收纳** — 目录提取 + 批量转系列内容采集6h优质文档站无法离线保存、系统阅读T1603**目录导航直达** — TOC 标题点击即跳转阅读增强1h长文章章节切换靠手动滚动T1604剪贴板→便签全局快捷键（Phase 15 遗留）效率提升2.5h键盘流被打断T1605应用图标更新为 favicon.ico品牌收尾0.5h托盘/快捷方式仍为旧版占位图标**总计4 项核心 + 1 项收尾~14h**

> 若仅交付前 3 项（**T1601 + T1602 + T1603**），工时 **11h**，即可覆盖本次全部重点。

* * *

## **二、逐项详细方案**

### **T1601 — 博客阅读即编辑：内嵌编辑器 + 位置记忆 🔴 P0 · 4h**

#### **用户现状**

*   阅读博客时看到要改的地方 → 点击“编辑” → 页面跳转至独立编辑页 → 光标在顶部 → 重新手动翻找刚才的段落 → 改完保存 → 返回阅读页 → 再次滚动找回阅读位置。
    
*   长博文（>3000 字）一次修改耗时可达 30 秒以上，打断心流。
    

#### **期望行为**

*   在阅读页面内，点击“编辑”按钮 → 当前页面平滑切换为编辑器视图，光标自动定位到刚才阅读的段落附近。
    
*   保存后自动切回阅读视图，页面仍停留在原位置。
    

#### **技术方案（仅改动** `BlogPreviewPage.tsx` **和路由）**

1.  **路由合并**：删除单独的 `/blog/:id/edit` 路由，改为 `/blog/:id?mode=edit`。`BlogPreviewPage` 根据 URL 参数切换视图。
    
2.  **位置记录**：阅读模式下，监听滚动位置，计算 `scrollRatio = scrollY / scrollHeight`。切换时将此比率传入编辑器。
    
3.  **编辑器定位**：编辑器加载内容后，按 `scrollRatio` 自动滚动到对应位置（使用 Tiptap `scrollIntoView()` 或简单的 `setTimeout` 滚动）。
    
4.  **平滑过渡**：视图切换用 CSS opacity 过渡（200ms），无需额外动画库。
    
5.  **保持现有逻辑**：全屏新建博客（`/blog/new`）不受影响；所有现有功能（草稿、附件、标签）保持不变。
    

#### **验收标准**

*   阅读页点击“编辑”→ 当前页平滑变为编辑器，且视图停留在阅读位置。
    
*   保存后自动返回阅读视图，阅读位置不变。
    
*   浏览器前进/后退按钮行为正常，无死循环。
    

#### **风险评估：低**

纯前端改动，不涉及数据流、存储或 IPC 变更。仅需处理 `useEffect` 清理，防止内存泄漏。

* * *

### **T1602 — 在线手册一键收纳：文档站 → 博客系列 🟡 P1 · 6h**

#### **用户现状**

*   浏览器遇到优秀的在线教程（如《Rust 程序设计》《Kubernetes 指南》），带有清晰的左侧导航目录，但由于是多页结构，无法用“收藏网页”功能抓取，只能手动一页页保存，工作量巨大。
    
*   导致大量有价值内容停留在 Web 上，无法整合进本地知识库。
    

#### **期望行为**

*   提供一个新入口：“收藏在线手册”。输入手册首页 URL → 自动分析目录结构 → 批量抓取所有页面 → 转化成一个带有前后导航的本地博客系列。
    
*   支持 mdBook、Docusaurus、VuePress、GitBook 等主流文档框架的目录格式，也兼容通用导航栏。
    

#### **技术方案（复用现有 WebScraperService + 博客系列能力）**

1.  **目录提取器**（前端+主进程配合）：使用 cheerio（已安装）对首页 HTML 进行 DOM 分析。识别常见框架特征（如 `<nav class="sidebar">`、`<ul class="menu">`），提取出一组 `[{title, url}]`。
    
2.  **批量抓取**：复用 `WebScraperService` 的单页抓取逻辑（Readability + turndown），按目录顺序逐个抓取，内置 500ms 礼貌延迟，并发限制为 2。
    
3.  **系列生成**：所有页面抓取完毕后，自动创建一个新的博客系列（设置 series 字段），按目录顺序生成 prev/next 链。每一页成为一篇独立 Markdown 博客。
    
4.  **进度与容错**：迷你进度窗口显示“正在抓取 3/15…”。失败页面自动跳过，完成后给予摘要报告（成功/失败计数）。
    
5.  **入口**：托盘菜单、桌宠菜单新增“📘 收藏在线手册”选项；主窗口“网页收藏”功能旁增加“批量手册”Tab。
    

#### **验收标准**

*   输入一个 mdBook 风格教程的首页 URL，成功生成所有章节博客，并按顺序链接。
    
*   输入一个普通博客首页（无目录结构），自动降级为单页收藏。
    
*   某章节 404 或超时，跳过并继续，最终报告中列出失败链接。
    

#### **风险评估：中**

目标站点可能有反爬策略，但我们不加 headers 伪装，只做礼貌抓取；前端解析依赖 cheerio，无新依赖；抓取频率严格限制，不影响本地性能。

* * *

### **T1603 — 目录导航直达：点击 TOC 标题跳转 🟢 P2 · 1h**

#### **用户现状**

*   阅读博客时，右侧 TOC（Table of Contents）展示了文章的所有标题，但不可点击。用户想跳转到某个章节，必须手动滚动，无法利用 TOC 做快速导航。

#### **期望行为**

*   点击 TOC 中任意标题项，页面平滑滚动到对应章节位置。
    
*   当前所在章节高亮（随滚动自动更新），帮助用户感知位置。
    

#### **技术方案**

*   在渲染 TOC 的组件中，为每个标题项添加点击事件。
    
*   通过 DOM 查询找到对应标题元素（使用 `id` 或 `data-heading`），执行 `element.scrollIntoView({ behavior: 'smooth' })`。
    
*   利用现有的 `IntersectionObserver`（若有）或简单的滚动监听更新当前高亮标题项。
    
*   改动仅限于 `BlogPreviewPage`（TOC 渲染部分），预计新增代码 <30 行。
    

#### **验收标准**

*   点击 TOC 任意标题 → 页面平滑滚动至对应章节。
    
*   手动滚动时，TOC 中当前标题项高亮同步。
    

* * *

### **T1604 — 剪贴板→便签快捷键 🟡 P1 · 2.5h**

*Phase 15 遗留，细节不变。（描述同前）*

* * *

### **T1605 — 应用图标更新 🟢 P3 · 0.5h**

*用已确定的 favicon.ico 覆盖 resources/ 下的图标文件。*

* * *

## **三、为何 Boss 应批准这三项核心任务**

**决策理由说明高频刚需**长文修改、手册收藏是写作型用户每日发生的场景。改一处跳转、找位置，是一次次微小的摩擦积累。**用户留存**手册收纳能力让应用从“单页收藏”升级为“知识站点本地化”，直接碾压浏览器书签，形成难以替代的竞争力。**零架构风险**全部改动位于渲染进程单个页面或复用现有服务，不动 DB 结构，不动 IPC 协议，不碰沙箱安全。**工时可控**核心三项 11h，属于一个标准 Phase 的体量，与“大改动”不同，无不可预见的复杂度。**视觉收益**T1603 虽小，却让博客阅读体验立刻向专业文档阅读器看齐（如 Notion、语雀）。

* * *

## **四、排期建议**

建议在一个周期内集中交付：

text

```
T1603 (1h, 先做, 快速提升) → T1601 (4h, 重点交互) → T1602 (6h, 重点采集)
T1604 (2.5h) 和 T1605 (0.5h) 可穿插于主要任务间隙完成。
```

* * *

## **五、后续展望（不纳入本次）**

*   FTS5 全文搜索、嵌套文件夹等大型工程项推迟到 Phase 17，待本次用户体验闭环后再议。